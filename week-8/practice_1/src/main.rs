fn main() {
    

    // Using vec::new()
    let v : Vec<i64> = Vec::new();

    // printing the size of vector 
    println!("\nThe length of Vec::new is: {}",v.len());

    // using macro
    let p = vec!["Grace", "peace", "Moses", "Samuel","Thooney"];

    // printing the size of vector
    println!("\nThe lengthof vec marco is: { }",p.len());
}
